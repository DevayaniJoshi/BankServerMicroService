Requirement analysis of bank server:
FUNCTIONAL REQUIREMENT:

bank should be able to process (add, delete, get , update) transactions for acc(crud op for acc and trans)
able to load csv data from into acc db
we should be able to validate transaction
if transaction is invalid there should be a second model to find the reasoning behind it
ability to share the transactional logs data to the security
integrate agentic ai

NON-FUNCTIONAL REQUIREMENTS:
Ensuring data integrity
ability to process 1000000 transact

Requirement analysis of  FAke bank server

Dataset Ingestion
Transaction Parsing
Transaction Replay Mechanism

After a transaction occurs a notification will be sent to kafka and later will be processed.
Making of an endpoint where we can find past 'n' transactions of an account in a server.
making of an filter table to black or gray list an account (col: accid & blacklisting level); if an acc dosent exist in filterlevel it is bydefault whitelisted
Checking how many transactions are blocked, reviewed ,and rejected except successful transaction don't want id's but else want all id's